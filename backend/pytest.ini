[pytest]
testpaths = tests
markers =
    model: requires a real licensed model checkpoint (skipped without one)
    perf: performance guard (G5); excluded from the default suite
filterwarnings =
    ignore::DeprecationWarning
